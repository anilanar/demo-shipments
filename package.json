{
    "name": "shipments",
    "private": true,
    "devDependencies": {
        "concurrently": "^3.5.1"
    },
    "scripts": {
      "start": "concurrently \"cd web && yarn start\" \"cd client && yarn start\""
    }
}
