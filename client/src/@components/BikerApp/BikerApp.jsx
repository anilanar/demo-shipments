import React from 'react';
import { AppContainer } from '@components/AppContainer';

import { TodoList } from './TodoList';

export const BikerApp = () => (
  <AppContainer>
    <TodoList />
  </AppContainer>
);
